import { DualViewTimerPipe } from './dual-view-timer.pipe';

describe('DualViewTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new DualViewTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
